import { Flex, Spinner } from "@radix-ui/themes";
import React from "react";

const AppSpinner = () => {
  return (
    <Flex justify="center">
      <Spinner size="3" />
    </Flex>
  );
};

export default AppSpinner;
